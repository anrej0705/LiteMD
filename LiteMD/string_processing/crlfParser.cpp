#include "crlfParser.h"

/*
* Метод обрабатывает символы переноса строки в соответствии с принятым синтаксисом
* в markdown. В методе проводится последовательное проходеждение
*/
std::wstring crlfParser(std::wstring &raw)
{
	std::wstring buffer;
	for (uint32_t index = 0; index < buffer.size(); ++index)
	{
		//Ищем символы переноса строки
	}
	return buffer;
}
